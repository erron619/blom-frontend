<div id="panel-navbar-notifications" class="absolute top-[110%] right-0 w-96 rounded-xl bg-white dropdownFading">
    <div class="divide-y-[1px] divide-gray-200 m-4 overflow-y-auto scrollbar h-[512px]">
        <% for( let i = 0; i < 8; i++ ) { %>
            <% 
                let __open = "";
                if (i < 3) __open = "open=true";
            %>
            <button <%= __open %> class="w-full py-4 space-y-2 text-right group/notif group/seen">
                <div class="flex items-center gap-2">
                    <div class="shrink-0 group-open/seen:block hidden w-2 aspect-square rounded-full bg-red-500"></div>
                    <div class="grow text-sm font-medium line-clamp-1 group-hover/notif:text-green-400 transition-all">پیشنهاد ما برای شما کاربر عزیز!</div>
                </div>
                <div class="text-xs line-clamp-1 text-gray-400">لورم ایپسوم متن ساختگی با تولید سادگی نامفهوم از صنعت چاپ و با استفاده از طراحان گرافیک است.</div>
            </button>
        <% } %>
    </div>
</div>